{
  "name": "marketing-website",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -r dist",
    "build": "tsc",
    "start": "nodemon ./dist/server/www-kernel/run.js",
    "refresh": "npm run build && npm run start",
    "clean-refresh": "npm run clean && npm run build && npm run start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/react": "^17.0.3",
    "@types/react-dom": "^17.0.3",
    "enzyme": "^3.11.0",
    "jest": "^26.6.3",
    "typescript": "^4.2.3"
  }
}
